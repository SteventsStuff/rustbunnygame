+ add player sprite;
+ controlling via keyboard;
+ add food sprites;
+ add collision;
+ add player stats;
+ add enemies + collision;
+ big code refact (move to plugins);

- add player lives and death via events;
- add game loop reset;
- add pause;
- add win message;

- big code refact (Everything);

- add basic UI;
- add sound;

- big code refact (Everything, optional);

- add more animations;
- code-generated things (grass, etc);
- obsticles;

- big code refact (Everything);

- re-draw Everything
